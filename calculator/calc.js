
var initialValue = 0;

var history = [];

function add(v) {
    initialValue += v;
}

function sub(v) {
    initialValue -= v;
}

function mul(v) {
    initialValue *= v;
}

function div(v) {
    initialValue /= v;
}
