
// Operation concatenation
Calc.add(5).mul(5).div(2).sub(1);

// Get the final result
Calc.getResult();

// Undo the last step
Calc.undo();

// Perform another operation
Calc.mul(3);

// Get the new result
Calc.getResult();

// Reset the result
Calc.reset();
